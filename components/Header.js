import styles from '../styles/header.module.scss'

const Header = () => {
  return (
    <div className={styles.header}>
      <h1>SIGN AND SEND MESSAGE USING BLOCKCHAIN</h1>
    </div>
  );
};

export default Header;
